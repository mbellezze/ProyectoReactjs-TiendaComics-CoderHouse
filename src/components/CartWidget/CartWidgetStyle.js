export const cartWidgetStyle = theme => {
    return ({
        imagen: {
            [theme.breakpoints.between('xs', 'md')]:{
                width: '30px',
                height: '30px',
            }
        }
    })
}